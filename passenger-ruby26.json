{
  "description": "Create mjonuschat/baseimage Docker image",
  "variables": {
    "ruby_version": "2.6.2",
    "docker_username": "{{env `DOCKER_USERNAME`}}",
    "docker_password": "{{env `DOCKER_PASSWORD`}}"
  },
  "sensitive-variables": ["docker_password"],
  "builders": [
    {
      "type": "docker",
      "image": "mjonuschat/baseimage:latest",
      "commit": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "scripts/prepare.sh"
      ]
    },
    {
      "type": "chef-solo",
      "json": {
        "container_passenger": {
          "node": {
            "enabled": true,
            "version": "node_10.x"
          },
          "ruby": {
            "enabled": true,
            "versions": ["{{user `ruby_version`}}"]
          }
        }
      },
      "cookbook_paths": [
        "cookbooks"
      ],
      "run_list": [
        "container_passenger"
      ],
      "prevent_sudo": true,
      "skip_install": false
    },
    {
      "type": "shell",
      "scripts": [
        "scripts/remove-chef.sh",
        "scripts/cleanup.sh"
      ]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "mjonuschat/passenger-ruby26",
        "tag": "latest"
      },
      {
        "type": "docker-push",
        "login": "true",
        "login_username": "{{user `docker_username`}}",
        "login_password": "{{user `docker_password`}}"
      }
    ],
    [
      {
        "type": "docker-tag",
        "repository": "mjonuschat/passenger-ruby26",
        "tag": "{{isotime \"20060102\"}}"
      },
      {
        "type": "docker-push",
        "login": "true",
        "login_username": "{{user `docker_username`}}",
        "login_password": "{{user `docker_password`}}"
      }
    ],
    [
      {
        "type": "docker-tag",
        "repository": "mjonuschat/passenger-ruby26",
        "tag": "{{user `ruby_version`}}"
      },
      {
        "type": "docker-push",
        "login": "true",
        "login_username": "{{user `docker_username`}}",
        "login_password": "{{user `docker_password`}}"
      }
    ]
  ]
}
